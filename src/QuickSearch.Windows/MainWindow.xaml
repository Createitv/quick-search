<Window x:Class="QuickSearch.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:QuickSearch.Core;assembly=QuickSearch.Core"
        Title="QuickSearch" Icon="Assets/QuickSearch.ico" Width="700" Height="570"
        MinWidth="620" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Activated="Window_Activated"
        Closing="Window_Closing">
  <Window.InputBindings>
    <KeyBinding Key="Escape" Command="{Binding HideCommand}" />
  </Window.InputBindings>

  <Grid Margin="24">
    <Border Background="{DynamicResource PaperBrush}"
            CornerRadius="16"
            Padding="24">
      <Border.Effect>
        <DropShadowEffect BlurRadius="22"
                          Direction="270"
                          ShadowDepth="5"
                          Color="#27364F"
                          Opacity="0.12" />
      </Border.Effect>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel>
            <TextBlock Text="QuickSearch" Style="{StaticResource WindowTitleStyle}" />
            <TextBlock Margin="0,4,0,0"
                       Text="输入关键词，用 Everything 快速打开文件夹"
                       Style="{StaticResource SecondaryTextStyle}" />
          </StackPanel>
          <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
            <Border Style="{StaticResource PillStyle}" Margin="0,1,8,0">
              <StackPanel Orientation="Horizontal">
                <Ellipse Width="7" Height="7"
                         Margin="0,0,7,0"
                         VerticalAlignment="Center"
                         Fill="{DynamicResource SignalCyanBrush}" />
                <TextBlock Text="Everything 文件夹搜索"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SlateBrush}" />
              </StackPanel>
            </Border>
            <Button Content="设置"
                    Style="{StaticResource QuietButtonStyle}"
                    Command="{Binding ShowSettingsCommand}" />
          </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,16">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <Border Style="{StaticResource SurfaceCardStyle}" Padding="15,13">
            <StackPanel>
              <TextBlock Text="搜索文件夹" Style="{StaticResource FieldLabelStyle}" />
              <TextBox x:Name="SearchBox"
                       AutomationProperties.Name="搜索文件夹"
                       FontSize="17"
                       Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                  <KeyBinding Key="Up" Command="{Binding SelectPreviousCommand}" />
                  <KeyBinding Key="Down" Command="{Binding SelectNextCommand}" />
                  <KeyBinding Key="Enter" Command="{Binding OpenSelectedCommand}" />
                </TextBox.InputBindings>
              </TextBox>
              <TextBlock Margin="2,7,0,0"
                         Text="输入即搜索 · ↑↓ 选择 · Enter 打开"
                         Style="{StaticResource SecondaryTextStyle}" />
            </StackPanel>
          </Border>
          <Grid Grid.Row="1" Margin="14,-1,14,0">
            <Rectangle Height="3" RadiusX="1.5" RadiusY="1.5">
              <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                  <GradientStop Color="{StaticResource RouteBlueColor}" Offset="0" />
                  <GradientStop Color="{StaticResource SignalCyanColor}" Offset="1" />
                </LinearGradientBrush>
              </Rectangle.Fill>
            </Rectangle>
            <Ellipse Width="10" Height="10"
                     HorizontalAlignment="Right"
                     Fill="{DynamicResource SignalCyanBrush}"
                     Stroke="White"
                     StrokeThickness="2" />
          </Grid>
        </Grid>

        <Border Grid.Row="2"
                Margin="0,0,0,16"
                Padding="13,10"
                CornerRadius="10">
          <Border.Style>
            <Style TargetType="Border">
              <Setter Property="Background" Value="{DynamicResource MutedSurfaceBrush}" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding StatusKind}" Value="{x:Static core:StatusKind.Success}">
                  <Setter Property="Background" Value="{DynamicResource SoftCyanBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding StatusKind}" Value="{x:Static core:StatusKind.Error}">
                  <Setter Property="Background" Value="{DynamicResource SoftErrorBrush}" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Border.Style>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Ellipse Width="8" Height="8"
                     Margin="0,1,10,0"
                     VerticalAlignment="Top">
              <Ellipse.Style>
                <Style TargetType="Ellipse">
                  <Setter Property="Fill" Value="{DynamicResource SlateBrush}" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding StatusKind}" Value="{x:Static core:StatusKind.Success}">
                      <Setter Property="Fill" Value="{DynamicResource SignalCyanBrush}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding StatusKind}" Value="{x:Static core:StatusKind.Error}">
                      <Setter Property="Fill" Value="{DynamicResource ErrorBrush}" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Ellipse.Style>
            </Ellipse>
            <TextBlock Grid.Column="1"
                       Text="{Binding Status}"
                       TextWrapping="Wrap"
                       LineHeight="19"
                       Foreground="{DynamicResource InkBrush}" />
          </Grid>
        </Border>

        <Grid Grid.Row="3">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <Grid Margin="0,0,0,10">
            <TextBlock Text="文件夹" Style="{StaticResource SectionTitleStyle}" />
            <TextBlock HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Text="双击也可以打开"
                       Style="{StaticResource SecondaryTextStyle}" />
          </Grid>
          <ListBox Grid.Row="1"
                   ItemsSource="{Binding Results}"
                   SelectedItem="{Binding SelectedResult, Mode=TwoWay}"
                   ItemContainerStyle="{StaticResource ResultListItemStyle}">
            <ListBox.InputBindings>
              <KeyBinding Key="Enter" Command="{Binding OpenSelectedCommand}" />
              <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenSelectedCommand}" />
            </ListBox.InputBindings>
            <ListBox.ItemTemplate>
              <DataTemplate>
                <StackPanel>
                  <TextBlock Text="{Binding Name}"
                             FontSize="14"
                             FontWeight="SemiBold" />
                  <TextBlock Margin="0,4,18,0"
                             Text="{Binding FullPath}"
                             TextWrapping="Wrap"
                             Foreground="{DynamicResource SlateBrush}"
                             FontSize="12" />
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </Grid>

        <Grid Grid.Row="4" Margin="0,16,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Esc 隐藏"
                       VerticalAlignment="Center"
                       Style="{StaticResource SecondaryTextStyle}" />
            <Border Margin="12,0,0,0"
                    Padding="9,4"
                    Background="{DynamicResource MutedSurfaceBrush}"
                    CornerRadius="8">
              <TextBlock Text="{Binding ShortcutInstruction}"
                         FontSize="11"
                         Foreground="{DynamicResource SlateBrush}" />
            </Border>
          </StackPanel>
          <Button Grid.Column="1"
                  Content="打开选中文件夹  →"
                  IsDefault="True"
                  Style="{StaticResource PrimaryButtonStyle}"
                  Command="{Binding OpenSelectedCommand}" />
        </Grid>
      </Grid>
    </Border>
  </Grid>
</Window>

