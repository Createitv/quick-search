<Window x:Class="QuickSearch.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuickSearch" Width="620" Height="560"
        MinWidth="520" MinHeight="460"
        WindowStartupLocation="CenterScreen"
        Activated="Window_Activated"
        Closing="Window_Closing">
  <Window.InputBindings>
    <KeyBinding Key="Escape" Command="{Binding HideCommand}" />
  </Window.InputBindings>
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Margin="0,0,0,12">
      <Button DockPanel.Dock="Right" Content="设置"
              Command="{Binding ShowSettingsCommand}" />
      <StackPanel>
        <TextBlock Text="QuickSearch" FontSize="26" FontWeight="SemiBold" />
        <TextBlock Text="{Binding ShortcutInstruction}" Foreground="#666" />
      </StackPanel>
    </DockPanel>

    <StackPanel Grid.Row="1">
      <TextBlock Text="邮箱别名" FontWeight="SemiBold" />
      <TextBox x:Name="AliasBox" FontSize="17"
               Text="{Binding Alias, UpdateSourceTrigger=PropertyChanged}" />
      <TextBlock Text="真实文件夹名称" FontWeight="SemiBold" Margin="0,8,0,0" />
      <DockPanel>
        <Button DockPanel.Dock="Right" Content="立即搜索"
                Command="{Binding SearchCommand}" />
        <TextBox FontSize="17"
                 Text="{Binding FolderQuery, UpdateSourceTrigger=PropertyChanged}">
          <TextBox.InputBindings>
            <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
          </TextBox.InputBindings>
        </TextBox>
      </DockPanel>
    </StackPanel>

    <Border Grid.Row="2" Background="#F3F5F7" CornerRadius="6" Padding="12" Margin="0,10">
      <TextBlock Text="{Binding Status}" TextWrapping="Wrap" />
    </Border>

    <ListBox Grid.Row="3"
             ItemsSource="{Binding Results}"
             SelectedItem="{Binding SelectedResult, Mode=TwoWay}">
      <ListBox.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding ConfirmOpenCommand}" />
        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding ConfirmOpenCommand}" />
      </ListBox.InputBindings>
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Margin="4">
            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
            <TextBlock Text="{Binding FullPath}" Foreground="#555" TextWrapping="Wrap" />
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

    <TextBlock Grid.Row="4" Margin="0,10,0,4"
               Text="{Binding SelectedResult.FullPath}"
               TextWrapping="Wrap" Foreground="#444" />

    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="隐藏" Command="{Binding HideCommand}" />
      <Button Content="确认并打开" IsDefault="True"
              Command="{Binding ConfirmOpenCommand}" />
    </StackPanel>
  </Grid>
</Window>
